services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped      # ← keeps running after terminal close / reboot
    environment:
      POSTGRES_USER: closeteye
      POSTGRES_PASSWORD: closeteye
      POSTGRES_DB: closeteye
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  backend:
    build: ./backend
    restart: unless-stopped      # ← auto-restarts on crash; survives terminal close
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    depends_on:
      - db
    volumes:
      - ./backend/app:/app/app
      - ./backend/firebase-service-account.json:/app/firebase-service-account.json
      - uploads:/app/uploads

volumes:
  pgdata:
  uploads:
